language: go

jobs:
    include:
        - stage: Fuzz regression
          go: 1.12.x
          dist: bionic
          script:
              - cd fuzztest
              - sh fuzzit.sh
        - stage: Fuzz
          if: branch = master AND type IN (push)
          go: 1.12.x
          dist: bionic
          script:
              - cd fuzztest
              - sh fuzzit.sh fuzztest

env:
    GO15VENDOREXPERIMENT=1
